type SFHome {
    id: ID!
    floors: Int!
    area: Int!
    chimney: Boolean!
}

type Condo {
    id: ID!
    floors: Int!
    elevator: Boolean!
}

union Building = SFHome | Condo

extend type Project {
    buildings: [Building!]!
}